// What will be logged and why?

let str = '';

while (str.length < 10) {
  str += '*';
  console.log(str);
}

/*
`str` is initialized to the `''`. within the while loop 

Within the while loop `str` is concatenated with string `'*'`, and log to the console.

*/